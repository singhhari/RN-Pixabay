// native components
import React from 'react';
import {
    View,
    SafeAreaView,
} from 'react-native';
import PropTypes from 'prop-types';

const SearchDetailPresentational = () => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <View style={{flex: 1, backgroundColor: 'orange'}}>

            </View>
        </SafeAreaView>
    );
}

export default SearchDetailPresentational;